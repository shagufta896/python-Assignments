# Input a string
string = input('Enter string: ')

# Calculate the length of the string
length = len(string)

# Print the string 'length' times
result = string * length
print("Result:", result)

